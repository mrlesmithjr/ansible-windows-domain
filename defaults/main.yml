---
# defaults file for ansible-windows-domain

# Defines whether or not RSAT AD Admin Center is installed
windows_domain_admin_rsat: false

windows_domain_info:
  dns_domain_name: vagrant.local
  safe_mode_password: Vagrant@123!

reboot_timer: 3600
shutdown_timer: "{{ reboot_timer }}"
